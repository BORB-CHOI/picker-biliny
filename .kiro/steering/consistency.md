---
inclusion: fileMatch
fileMatchPattern: "**/*.tsx,**/*.css"
---

# 일관성 규칙 — 기존 코드와의 일관성 유지

새 섹션/컴포넌트를 개발할 때 반드시 기존 코드베이스와 동일한 톤을 유지해야 한다.
Figma 디자인 데이터에 있는 값을 그대로 복사하지 않고, 이 프로젝트에서 실제로 사용 중인 패턴을 기준으로 변환해야 한다.

## 개발 전 필수 확인 절차

새 섹션을 만들기 전에 반드시 기존 섹션 2개 이상을 읽고:
1. 어떤 CSS 클래스 패턴을 사용하는지
2. 어떤 폰트/색상/간격 체계를 쓰는지
3. 어떤 애니메이션 클래스와 효과를 쓰는지
4. `globals.css`에 정의된 기존 클래스와 충돌하지 않는지

## 금지 사항

- Figma 값 직접 복사 금지 (font-family, text-shadow 등)
- 기존에 없는 시각 효과 임의 추가 금지
- `font-family` 직접 지정 금지 — body의 Inter를 상속받으면 됨
