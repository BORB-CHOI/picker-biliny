'use client';

/**
 * SolutionSection — 해결책 제시 (Figma node: 4062:3)
 *
 * ┌─────────────────────────────────────────────────┐
 * │  "이에 대한 해결책을 제시합니다"  (볼드 11px)    │
 * │                                                  │
 * │  ═══ 가치 1 ═══                                  │
 * │  빌려타는 "빌리니"  (볼드 11px)                  │
 * │  "필요할 때 언제든지 부르시면 빌려드립니다.       │
 * │   집 앞까지 와주고,                              │
 * │   가고자하는 모든 길을 갑니다.                    │
 * │   모든 여정을 마치면 스스로 돌아갑니다."          │
 * │  (7px, #656565, 줄간격 9px)                     │
 * │                                                  │
 * │  ═══ 가치 2 ═══                                  │
 * │  서로에게 안전한 "안내길"  (볼드, 우측정렬)       │
 * │  "가는 길이 약속된 '안내길'을 깔아드려요.        │
 * │   탑승자/보행자/운전자 모두를 안전하게 안내"      │
 * │  (7px, #656565, 우측정렬)                       │
 * │                                                  │
 * │  ═══ 가치 3 ═══                                  │
 * │  모든 과정을 지켜봐주는 "돌봄이"  (볼드)         │
 * │  (이 섹션은 텍스트만 — 상세 내용은 별도 섹션)    │
 * │                                                  │
 * │  ═══ 가치 4 ═══                                  │
 * │  모두에게 "공평한" 모빌리티  (볼드 11px)         │
 * │  "모두가 타실 수 있어요.                         │
 * │   학생부터 고령자까지 모두 이용할 수 있습니다."  │
 * └─────────────────────────────────────────────────┘
 *
 * 에셋:
 * - /images/solution/ (빌리니, 안내길, 공평 관련 이미지)
 *
 * TODO:
 * [ ] 4가지 핵심 가치 레이아웃 (빌리니, 안내길, 돌봄이, 공평한)
 * [ ] 각 가치 블록의 제목 + 설명 텍스트
 * [ ] 좌-우 교차 정렬 (빌리니=좌, 안내길=우, 돌봄이=좌, 공평한=좌)
 * [ ] GSAP ScrollTrigger — 스크롤에 따라 각 가치 블록 fade-in
 * [ ] 관련 일러스트/이미지 배치 (현재 Figma에서는 비어있음 — 영상/이미지 추가 필요)
 * [ ] 반응형 레이아웃
 */

import { useRef } from 'react';

export function SolutionSection() {
  const sectionRef = useRef<HTMLElement>(null);

  return (
    <section
      ref={sectionRef}
      className="relative py-24 md:py-32"
    >
      {/* TODO: 구현 */}
    </section>
  );
}
